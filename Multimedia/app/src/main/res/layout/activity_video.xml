<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="mundo.multimedia.VideoActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Buscar video"
        android:id="@+id/btnBuscarVideo"
        android:layout_toLeftOf="@+id/imageBtnPlay"
        android:layout_toStartOf="@+id/imageBtnPlay" />

    <VideoView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/videoView"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/btnBuscarVideo" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageBtnPlay"
        android:src="@android:drawable/ic_media_play"
        android:layout_alignBottom="@+id/btnBuscarVideo"
        android:layout_alignRight="@+id/videoView"
        android:layout_alignEnd="@+id/videoView" />

</RelativeLayout>
